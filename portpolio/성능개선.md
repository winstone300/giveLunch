# 성능 개선

**검색어 추천 기능 성능 개선**

---

`Java` `Spring Boot` `JPA` `MySQL` `Prometheus` `Grafana`

**API 응답 속도 단축 및 서비스 안정성 확보**

**Background**

---

- 신규 기능으로 음식 메뉴 추가 시 유사 이름의 음식 추천해 주는 기능 추가
- 쿼리 질의 시간이 길어지며 커넥션 풀을 오래 점유하는 문제 발생

**Approach**

---

**기존 쿼리**

```java
@Query("""select f from Food f
					where f.name like concat('%', :name, '%')      
					order by length(f.name) asc
					""")
```

**Problem1.** 부분 문자열 검색 LIKE '%keyword%’ 으로 인덱스를 못 탐 

**Problem2.** 정렬 로직으로 인한 처리 시간 증가

**SOL1-1.** elasticSearch 사용 (X)

→ 메모리 비용 및 구현 복잡도 증가

→ 추가적인 정합성 보장 로직을 구현해야 됨

**SOL1-2.** 역색인 방식 인덱스 설계 (X)

→ db 용량이 너무 커짐 , disk I/O 비용 증가

→ sol2-2. FULLTEXT 방식 : 토큰화/파서에 영향을 많이 받아 정확성 보장이 불확실

**SOL1-3**. prefix 검색으로 변경 (O)

→ 검색 기능이 아닌 추가 기능을 보조하는 역할이기에 prefix 방식으로 충분할 거라 판단

**SOL2.** 정렬 로직 앱에서 처리하도록 변경 (O)

→ 정렬 처리는 DB가 아닌 앱에서 처리하도록 하여 커넥션 풀 점유 시간 단축

→ limit을 걸지 않은 대신 두 글자 이상  부터 추천 검색 실행

```java
@Query("""select f from Food f
					where f.name like concat(:name, '%')
					""")
```

**Results**

---

- 로직을 앱으로 이동하였기에 단순 쿼리 성능 뿐 아니라 api 성능 평가도 실행
- api 성능은 grafana로 측정 한 p95, 쿼리 성능은 performance_schema 기준 평균 실행 시간으로 함
- db에는 대용량 데이터 상황을 재현하기 위해 각 메뉴 별 10 만건의 데이터를 임의로 추가
    
    (김치찌개_0 , 김치찌개_1 , … , 김치찌개_99999)
    

|  | 초기 | 인덱스 개선 후 | 최종 |
| --- | --- | --- | --- |
| **API** | 8.2 s | 2.8 s | 2.6 s |
| **Query** | 6,756.54 ms (약 6.76초) | 1,541.98 ms (약 1.54초) | 2.85 ms |
